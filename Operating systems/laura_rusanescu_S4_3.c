#include <stdio.h>
#include <stdlib.h>

int makeSum(int argv[]){
	int s = 0;
	for (int i=0; i<legth(argv); i++){
		s= s+ argv[i];
	}
	return sum;
}

int main(int argc, char *argv[])
{
    if (argc < 2)
    {
        printf("Invalid");
        return 1;
    }

    int arr [argc];
    for (int i = 0; i < argc; i++) { 
        while (argv[i] != NULL) {
            strcpy(arr[i], argv[i]);
        }
    }

    for (int i = 0; i < argc; i++) {
        for (j = 0; j <= i; j++) {
            int arrS[argc];
            priftf(makeSum(arrs));
        }
    }
    if (line)
        free(line);
    
}
